.env
.env.local
node_modules

# Separate product/web app kept out of the public distributed repo
web/
dist/
.next

# Nested backend repo (node gateway) kept out of the distributed repo
src/server/

# GitHub Pages deploy working copy
.pages-deploy/

# Native packaging artifacts
dist/native/
dist/checksums/
target/
obfuscation-map.txt
proguard_seed.txt
